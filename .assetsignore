# Only upload the static book — never .git (25 MiB pack limit)
.git
.git/
**/.git/**

.github/
.wrangler/
node_modules/
dist/
public/
scripts/

# Worker / tooling (not public site files)
src/
package.json
package-lock.json
wrangler.jsonc
wrangler.toml
.assetsignore
.gitignore
.nojekyll
README.md

*.pack
*.idx
*.rev
*.bitmap
.DS_Store
*.log
